# New Hope Band - Songbook PWA (project context for Claude)

A worship songbook PWA for a church team. Static site, no build step. Author: Avdey Axonov. MIT licensed.

## Hosting / deploy
- **GitHub Pages**, auto-deploys from branch **`main`** (develop & push here). `main` and `claude/kind-mendel-wSAqa` were merged into one history; keep both in sync (push to both) until the `kind` branch is retired.
- After ANY change: bump `CACHE_VERSION` in `service-worker.js` AND the build stamp `#ver` in `index.html` (footer, "build N"). The footer build number is how the user verifies the live version (GitHub Pages CDN + service worker can serve stale files ~10 min).
- Commit + push every change to the branch. Validate JS with `node --check app.js`.
- **Update this CLAUDE.md with every change**: keep the docs above accurate AND add a line to the Changelog at the bottom (build N - what/why). It's the project's history + knowledge base.

## Files
- `index.html` - all markup + CSS (single `<style>`). Views: list (#list-view) and song (#song-view); overlays: #editor, #login, #share; floating controls bubble #fab-wrap.
- `app.js` - all logic, one big IIFE. No framework. `$ = getElementById`.
- `convert-core.js` - "chords above lyrics" → ChordPro converter (used by app + converter.html). `smartImport(text, {title,key,german})` auto-detects the input: **ChordPro** (has `[chords]`/`{directives}`) passes through; **aligned** chords-above-lyrics goes through `convert()`; the **mangled per-line website copy** (each chord + each lyric fragment on its own line) is rebuilt by `reconstructInterleaved()` (chords held and attached to the following word; a capitalised fragment starts a new line; `guessKey()` infers the key from the first chord). The editor's **"Format pasted text"** buttons call it.
- `songs.js` - `window.SONGS` array; now only an **offline seed/fallback**. Source of truth is Supabase.
- `service-worker.js` - network-first for app shell (cache:'no-store'), cache-first for vendor/icons/songs.js (songs.js is just the offline seed; a version bump re-precaches it).
- `converter.html` - standalone admin tool (paste sheet → ChordPro block, incl. bilingual).
- `migrate.sql` - recreates the Supabase `songs` table + loads songs.js. Plus run these ALTERs:
  `alter table public.songs add column if not exists num serial;`
  `alter table public.songs add column if not exists tags text;`
  `alter table public.songs add column if not exists prev jsonb;`
- `chord-diagrams.js` - offline guitar chord diagrams: `window.ChordDiagram.svg("Am7")` → SVG string or null (open shapes preferred, movable barre fallback from the root; unknown qualities → null and the popover shows just the name).
- `icons/`, `manifest.json`, `vendor/chordsheetjs.min.js`.

## Supabase (global catalog)
- URL + **anon key** are embedded in `app.js` (anon is public/safe; writes need login + RLS).
- Table `songs(id uuid, num serial, title text, data jsonb, src text, tags text, prev jsonb, created_at)`.
  - `data` = a SONGS entry: a ChordPro **string** (single language) or `{title, versions:[{lang,text}]}` (bilingual).
  - `src` = JSON of the editor's friendly inputs (for round-trip editing).
  - `prev` = the data before the last save (Restore previous).
  - song uid in-app = `"g:" + num`.
- RLS: public read; insert/update/delete require an authenticated user. Sign-ups disabled; admins added in dashboard.
- Auth: email/password via `/auth/v1/token`; **remember-me** stores refresh token, auto-refresh (`ensureAuth`).
- Fetch is resilient: selects fall back if `prev`/`tags` columns are missing (`hasPrevCol` gates writing `prev`).
- `refreshGlobal()` loads + caches to localStorage `globalsongs`; `build()` uses cache (else songs.js) + device `usersongs`. **Skips** the re-normalize + list re-render when the fetched catalog is byte-identical to the cache the list was already built from (avoids a rebuild flash on every launch).
- **Lazy parse:** `normalize()` reads each version's title/key by regex (cheap) and defers the ChordSheetJS parse; `ensureParsed(v)` runs the parse (and caches it on the version) the first time a song is opened. Anything touching `v.parsed` (renderSheet, bakeTranspose, saveChordEdit) calls `ensureParsed(v)` first. Saves parsing the whole catalog on every load + refresh.

## Formatting rules (in `standardize()` / `app.js`)
- Section labels auto-detected (any language) → English `{comment:}` (Verse/Chorus/**Alt Chorus**/Bridge/Intro/Half-Chorus/**Pre-Verse**/**Pre-Chorus**/**Pre-Bridge**…) — universal like chords, both on display (`standardize`/`englishLabel`) and in the converter (`convert-core` `SECTION` map). `[VERSE]` bracket labels too.
- Repeat markers normalised to `xN` in any notation/order: `2x`, `х2`, `(2x)` → `x2` (`normRepeat`, in headers and on chord lines). The `2x` form must be adjacent (no space between digit and `x`) so a section number followed by a separate repeat - `Verse 1 x2` - keeps both (`1` = number, `x2` = repeat) instead of mis-reading `1 x` as the repeat (`englishLabel`/`headerLabel` repeat regex is `\d+[xх]` not `\d+\s*[xх]`).
- `N.C.` (no-chord) is treated as a chord: `NC`/`N.C`/`(N.C.)` → `[N.C.]`, shown in the chord row (ChordSheetJS renders + transposes it untouched).
- **Notes/annotations:** a `*` marks a small italic muted annotation (Inter, 0.82em - not the label style). Two placements: **inline** with a section header (`Verse 1 *softly` or `Chorus x4 *3rd accapella`) renders the note beside the label; **standalone** on its own line (`*building`) renders as a small block note. `standardize` splits a trailing `*note` off a header and folds it - plus any following `*` lines - into the header's own `{comment: LABEL *note}` (emitted BEFORE `{start_of_chorus}` so a chorus note sits next to the label, not inside the tinted card); a lone `*` line with no preceding header stays a `{comment: *...}` block. `renderSheet` splits each `.comment` at `*`: text before the first `*` is the label, each `*`-chunk after becomes a `.note-inline` span (or the whole thing is a `.note` block if it starts with `*`), stripping the `*`. `tidyBlanks` adds a blank before label comments but not note-only ones, so a note hugs its section.
- Blank lines appear only BETWEEN sections, never within a verse/chorus/bridge: `standardize` drops intra-section blanks (the chorus-body reader skips them) and re-inserts one before each section header; the converter does the same via `tidyBlanks`.
- Chord-only lines auto-bracketed; bar lines `|` and beat dots stripped from them.
- `transformLine()` collapses a space after a stand-alone chord (`me [G] close` → `me [G]close`) but keeps it when the chord is glued to the previous word (`me[G] close` stays - else the words merge to "meclose").
- `renderSheet()` converts leading/trailing spaces of each `.lyrics` column to non-breaking spaces: the browser trims edge spaces at a flex-column boundary, which otherwise glues words split by a chord.
- Parenthesised chords `(Fm)` supported (transposed post-render since ChordSheetJS can't parse them).
- Impossible enharmonics fixed (Cb→B, Fb→E, B#→C, E#→F).
- `fixMaj()` (display only): ChordSheetJS's HtmlDivFormatter abbreviates a major-7 quality on render (`Amaj7`→`Ama7`); restore `maj` for display. The stored ChordPro keeps `maj7` (ChordProFormatter doesn't abbreviate). The emitted `ma` is always followed by a digit, so minor (`m7`) and `madd9` are untouched.
- Bilingual: English version shown first.
- `chordproToSheet()` reverses ChordPro → friendly sheet for editing old songs; it forces one blank line before each section label so the editor (and the "Format" button) never squishes a verse into the next chorus, idempotently.

## Design system (UI)
- **Font:** `Inter` for all UI + body (covers Latin **and** Cyrillic so English/Ukrainian match); `JetBrains Mono` for chords/keys/badges. (Replaced Hanken Grotesk, which lacked Cyrillic.)
- **Themes:** light = warm paper/ivory; dark = cool near-black. Indigo (light) / periwinkle (dark) accent. Single **flat** `--bg` color (no gradient - it used to read as bands). `theme-color` meta updated in `applyTheme()`.
- **Duotone accent:** `--accent-grad` (indigo→violet) on the FAB, progress bar, and active All/Set + language tabs.
- **Glassmorphism:** `--glass-bg`/`--glass-border`/`--glass-blur` tokens (frosted translucent + blur) on the header, FAB control panel, phone bottom-nav, and modal backdrops.
- **Flat/borderless list:** rows are one continuous column with hairline dividers (no per-row cards). No index numbers.
- **Pills:** search, icon buttons, control/row/editor buttons are fully rounded; circular buttons center their glyphs (`grid` + `line-height:0`).
- **Stage mode:** forces a true-black OLED palette regardless of theme (`html.stage` var overrides).
- `scrollbar-gutter: stable` on `<html>` so the centered layout never shifts when the list grows tall enough to scroll.
- Editor bottom-sheet height is capped with `dvh` minus `safe-area-inset-top` so Save/Close clear the iPhone notch.

## Layout / navigation
- **Phone (≤640px):** All/Set tabs dock as a fixed bottom-nav bar (moved OUT of `<header>` because the header's `backdrop-filter` traps `position:fixed`). **Swipe left/right on the list** switches All↔Set (animated slide); tap also works.
- **Search:** always-visible custom clear (×) button (`#search-clear`), toggled in JS on input. **Universal**: a query searches the whole catalog from either tab - on the Set tab a search shows all matching songs (with the add-to-set `+` button so you can pull one in), and clearing the query returns to the set list (reorderable). `renderList` gates this via `const setView = listMode === "set" && !q` (set-only base + reorder/remove tools only when `setView`).
- **Set reorder:** drag the grip handle (mouse), OR **press-and-hold anywhere on a row** (touch). Hold-drag floats a *clone* while the real row stays in the DOM as a hidden traveling gap (avoids iOS `touchcancel`); rows are `user-select:none`.
- **Resume to list + back gesture:** the app **no longer auto-opens the last song at launch** - `restoreOpen()` instead scrolls the remembered song into view and briefly highlights it (`.just-open`). Reason: auto-opening put the app in the song view backed by a history entry created during page load, which Android Chrome *voids*, so the first Back gesture exited the app (only worked after a few seconds / an interaction - builds 42–43 tried deferring vs. syncing the push, neither fixed the immediate-back case). Landing on the list keeps Back predictable (back from the list exits, as expected); the last song is a one-tap reopen. Tapping a song still pushes `{view:'song'}` (a real user gesture, so the entry sticks) and Back returns to the list.
- **Editor + back/history:** opening the editor pushes an `{ed:1}` history entry so the phone back gesture closes the editor (not the song). On desktop (`hover:hover` + `pointer:fine`) a stray back - e.g. an overscroll while drag-selecting text right-to-left - is ignored (`editKeepOpen` re-pushes the entry) so edits aren't lost; close via the Close button. `overscroll-behavior-x:none` on `<html>` and `overscroll-behavior:contain` on the textareas also block the swipe-back at the source. **No click-outside-to-close** on the editor backdrop (a text selection that ends on the dimmed backdrop would otherwise fire a click there and discard edits) - the editor closes only via Close/Save/Delete (or the mobile back gesture).

## Features
search; tag chips + filter; favorites (local, row star + ★ filter); named sets (per service) with share link+QR + import; swipe between songs; swipe between All/Set tabs (menu); transpose (FAB **and** a quick-transpose bar `#quickbar` in the song header: −/key/+/reset) + admin "save key"; **tempo/BPM** (per-song `{tempo:N}` directive in the ChordPro; **tap-tempo** lives in the song header — tap `#qt-bpm` to the beat, updates autoscroll live and persists `{tempo}` a moment after the last tap for admins/local songs, session-only otherwise; the editor keeps the BPM in a hidden `#ed-bpm` so edits don't drop it); size +/−/reset + pinch; **autoscroll** (time-based; speed = `bpm×0.25×size×trim` px/sec when the song has a BPM, else a default; slow/fast buttons trim `scrollmult`); chords on/off; stage mode (OLED); PDF print (2-col, no headers); in-app add/edit/delete (global, bilingual); restore previous version; always-visible add-to-set + favorite in the song header; **notes** (a `*` line → small italic annotation under a section); back-to-top; remembers your tab + last song on refresh (resumes to the list with that song highlighted, not auto-opened - keeps the Android Back gesture predictable); editor closes on back gesture; **chord popover**: tap any chord → guitar diagram (follows transpose); pencil edit for admins (global songs; local songs need no login) - the edit is untransposed before saving so the stored ChordPro stays in the original key, `prev` is kept for restore.

### Chord popover internals (app.js)
- `renderSheet()` stamps `data-ci` (running index) on non-empty `.chord` spans; the same enumeration order over `v.parsed`'s non-empty `ChordLyricsPair`s locates the tapped chord (verified 1:1 incl. paren chords; empty pairs are skipped on both sides).
- `saveChordEdit()` mutates `pair.chords`, reformats with `ChordProFormatter` (standardized output, same precedent as bakeTranspose), matches the bilingual `data.versions` entry by text→lang→index (display order is English-first-sorted), writes `{data, src:null, prev}` via `sbWrite`, reverts the in-memory chord on failure.
- ChordSheetJS normalizes some names on render (`Gsus4` shows as `Gsus`); the stored text keeps what the user typed.

## localStorage keys
sets, activeSet, globalsongs, usersongs, favs, opensong, listmode, size, scrollmult (autoscroll trim), theme, chords, sb_token/sb_exp/sb_email/sb_refresh.

## Conventions
- Keep it simple, mobile-first, offline-friendly, free. Save tokens: small focused edits.
- Bump build stamp + SW version on every change; commit + push to `main` (and keep `kind` in sync).
- Update the Changelog below (and any stale docs above) with every change.

## Changelog
- **build 89** (2026-08-11) - FORMAT NOW ALIGNS CHORDS OVER THE RIGHT SYLLABLE. `chordproToSheet()` built the chord row by shoving a chord right whenever it collided with the previous one (`target = row.length + 1`) but NEVER padded the lyric to match - so the moment a chord was wider than the gap to the next one, the chord row outgrew the lyric row and every later chord drifted, losing the syllable it belonged to. Measured through the real Format button: `[Gmaj7]I [Cadd9/E]am [F#m7b5]here` put `Cadd9/E` at column 6 instead of 2 (nothing aligned at all), and `[G]Ho[D/F#]ly [Em]Lord [C]God Al[G]migh[D]ty` drifted 1-3 chars, landing `Em` over the "d" of "Lord" and `C` over the "o" of "God". Dense chord changes and long chord names are exactly what a worship chart is full of. FIX: the chord and lyric rows are now built TOGETHER, padding the lyric so each chord sits directly above its syllable - with a hard guard that the lyric is only ever stretched at a WORD BOUNDARY (`atWordStart`), because padding mid-word would turn "Almighty" into "Al    mighty", which is worse than drift; a mid-word chord drifts exactly as before, and those are short in practice. THE TRAP THIS NEARLY WALKED INTO: the editor's aligned text is converted BACK through `convert()` -> `mergeLines()`, which keeps whatever spaces are in the lyric - so the new alignment padding would have been SAVED into the song and rendered as gaps in the song view. `mergeLines` now collapses a run of 2+ spaces immediately before a chord bracket back to one (`/ {2,}(\[)/g`), which is safe precisely BECAUSE the padding only ever lands between words. Verified live: both failing inputs now align column-perfect, output is idempotent, and sheet -> ChordPro -> sheet is stable across three generations with the stored ChordPro byte-identical to the original (`[G]Ho[D/F#]ly [Em]Lord [C]God Al[G]migh[D]ty`) - no padding reaches storage. **FOUND BUT NOT FIXED HERE (pre-existing, confirmed by testing the same song against stashed code): `[Verse 1]`-style BRACKETED section labels are read as CHORDS by `smartImport`'s ChordPro detection**, so the first Format press renders them as a chord row with no blank line before the section, and a SECOND press is needed before they become real `{comment:}` labels with section spacing. Same class as the build-52 double-click bug. Blank-line counts go 19 -> 0 on the first press for such songs.
- **build 88** (2026-08-11) - SECTION STYLING REDESIGNED (build 87's look was rejected by the owner, correctly). WHY 87 FAILED: four different left-border styles is fussy, and **dashed and dotted borders are the visual language of placeholder/disabled states** - using them for musical structure made the sheet look unfinished rather than designed. The chorus "card" (tint + border-radius + padding) boxed up text that should flow, fighting the typographic rhythm build 76 established. And the whole signal sat at the far-left edge, away from where the eye actually lands. THE NEW PRINCIPLE: **the LABEL carries the identity** - it is the first thing in every section and where you look when scanning for structure - and only TWO families get a block marker, always a clean SOLID bar. (1) The dashed `.sec-lead` and dotted `.sec-instr` block rules are DELETED; those families are now label-only. (2) `.sec-chorus` is no longer a card: `border-radius` is gone (the radius is what made it read as a box) and it is now a full-width highlight BAND - `background: var(--chorus)` plus a 4px `::before` bar in `--accent-grad`, the existing duotone already used on the FAB and progress bar. (3) `.sec-bridge` keeps a quiet 2px solid `--muted` bar. (4) LABEL HIERARCHY - accent = chorus, plain = sung (verse/bridge), muted = lead-in (Pre-*), muted+italic = played-not-sung (Intro/Interlude/Solo/Outro/...). **A BUG CAUGHT IN VERIFICATION:** the first cut of this was INERT for three of five families - `.sheet .comment` already set `color: var(--accent)` on EVERY section label, so "accent = chorus" distinguished nothing; measured live, the chorus, verse and bridge labels were all identically `rgb(79,70,229)`. Fixed by changing the `.sheet .comment` BASELINE to `var(--text)`, which is what gives the accent meaning. Everything else in that rule (JetBrains Mono, 1.15em, 700, uppercase, 0.1em tracking, the build-76 hairline) is untouched, and the print override stays black. Verified live in light, dark AND stage: three distinct label levels in every theme, the plain label measures 13.12:1 contrast, the chorus bar renders 4px with a gradient, `border-radius` computes 0px on every section, no `dashed`/`dotted` remains on any `.sec-*` selector, and Pre-Chorus renders muted with no block marker. Print keeps its solid black chorus border with the gradient bar suppressed via `::before { display: none }`.
- **build 87** (2026-08-11) - SONG SECTIONS ARE NOW VISUALLY DISTINCT, and a long-dormant bug is fixed in the process. **The chorus tint had NEVER rendered.** `.sheet .paragraph.chorus` and `.sheet .paragraph.bridge` have existed since ~build 76, `--chorus` is defined in light/dark/stage, and `@media print` even carried a `.paragraph.chorus` override - but NOTHING ever applied those classes. A live DOM class census returned only `chord, column, comment, lyrics, paragraph, row`: zero `.chorus`, zero `.bridge`. Every paragraph had rendered identically for 25 builds while build 76's own changelog reasoned about "not fighting the chorus tint". Same failure class as build 81's duplicate `.ed-hint` rule - code that exists, reads correctly and silently does nothing. (1) THE TAGGER: `renderSheet()` now walks every `.paragraph` and adds one family class based on the START of its first `.comment` label (the label is the paragraph's own first row, verified in the DOM), skipping `.comment.note` standalone annotations. Order matters - `/^pre-/` is tested FIRST so "Pre-Chorus" lands on the lead-in rule instead of the chorus rule. Matching the START is what makes "Verse 1 x2", "Bridge 1", "Chorus x4" and a trailing `.note-inline` span all work. It runs at the end of `renderSheet`, so it survives transpose, language switch, size changes and stage mode for free. Families: `sec-lead` (Pre-*), `sec-chorus` (Chorus/Alt Chorus/Half-Chorus/Refrain), `sec-verse` (Verse/Half-Verse), `sec-bridge` (Bridge/Channel/Breakdown), `sec-instr` (Intro/Instrumental/Interlude/Solo/Turnaround/Outro/Tag/Vamp/Hook). Unmatched paragraphs get no class. (2) THE DESIGN RULE: **line style encodes function - solid = sung, dashed = lead-in, dotted = played** - and ONLY the chorus gets a fill, so chord/lyric contrast is untouched everywhere else. `sec-chorus` keeps the accent card (`--chorus` + 3px accent border + `--r-md`); `sec-bridge` 2px solid `--muted` (upgraded from `--border`, which was too faint to read); `sec-lead` 2px dashed `--muted`; `sec-instr` 2px dotted `--muted` plus a `--muted` label, since those are chord-only landmarks you skim rather than sing. `sec-verse` deliberately gets NO rule - the verse is the baseline and stays plain so choruses pop. The print override was renamed to `.sec-chorus` so printed sheets keep the card. Only existing tokens used, no new colours. Verified live across three songs: Intro/Interlude/Outro/Tag/Instrumental -> sec-instr, Verse -> sec-verse, Chorus -> sec-chorus, Bridge -> sec-bridge, and Pre-Chorus -> sec-lead (dashed) in "Blessed Be Your Name"; the chorus fill adapts per theme (light 0.08, dark 0.13, stage 0.16 alpha) and verse computes `0px none` in all three.
- **build 86** (2026-08-11) - "All" chip leads the tag-filter bar. The bar previously opened with the optional "★ Favorites" chip followed by the tags, so there was no explicit representation of the unfiltered state and no single control to clear a filter - you had to remember which chip was lit and tap it again to toggle it off. `renderTagBar()` now prepends an `All` chip as the first child, marked `active` precisely when `!activeTag && !favOnly`, so exactly ONE chip is ever highlighted and the resting state is visible rather than implied. It is a clear-all, not a toggle: it sets `activeTag = ""` AND `favOnly = false` (so it also escapes the Favorites filter, which the tag chips could not do), and tapping it while already active is a harmless no-op. The two guards that hide the whole bar are unchanged - in Set mode, and when there are no tags AND no favourites, since an "All" chip on its own would be pointless. Verified live: All leads the bar and is active at 112 songs; tapping "Bethel" de-highlights All and filters to 6; tapping All restores 112; tapping "★ Favorites" de-highlights All and filters to 1; tapping All restores 112 - with exactly one chip active throughout.
- **build 85** (2026-08-11) - Removed the "App updated - tap to reload" toast (owner's call - it was unwanted chrome). Deleted in both places it lived: the `#upd-toast` CSS block (including its `.show` state, its `b` accent rule and the `max-width: 640px` rule that lifted it clear of the phone bottom-nav) and the whole `controllerchange` listener in the inline registration script at the end of `<body>`, along with the `hadController`/`shown` guards that existed only to serve it. The service-worker REGISTRATION itself is untouched and still runs on `load` - offline support is unaffected; the app simply no longer announces its own updates. Verified live: the SW registers (`service-worker.js` active), the app boots all 112 songs, and `#upd-toast` no longer exists in the DOM. The footer build stamp remains the way to confirm which version is live, which is what the owner actually uses.
- **build 84** (2026-08-11) - ONE DESIGN SYSTEM, in three verified phases. Nothing was invented: every value below already existed in the file; the pass removes duplicates of tokens the app already had. Audit numbers are before -> after. (1) MOTION - the biggest offender, and the main reason the app read as assembled rather than designed: `--t-fast`/`--t-med`/`--ease` were all defined, yet the stylesheet carried EIGHT distinct durations and used the plain browser keyword `ease` **26 times** while `var(--ease)` was used exactly **ONCE**. Most of the app was animating on a curve nobody chose. Durations 8 -> 2 tokens (0.08/0.12/0.15 -> `--t-fast`; 0.18/0.2/0.28 -> `--t-med`), `ease` 26 -> **0**, `var(--ease)` 1 -> **26**. `linear` is untouched (7 uses) because linear is CORRECT for continuous motion (progress bars); the `0.001ms` reduced-motion kill switches are untouched; `rowResume 2.4s` and `hint-in 0.5s` are kept and commented as deliberate exceptions to the scale. (2) SHAPE - `border-radius: 999px` was hardcoded **22 times** while `var(--r-pill)` was used 6; now **0 hardcoded, 28 token uses**. A LEGACY third radius token `--radius: 14px` (used once, duplicating the concept of `--r-md: 12px`) is deleted and its usage repointed - a deliberate 2px change on one element. The 18px bottom-sheet/dialog corner is named `--r-lg` (an existing value) and used in 3 places incl. `var(--r-lg) var(--r-lg) 0 0`. `.ed-dot`'s `50%` stays a true circle per build 63's explicit call - it is a status dot, not a control. (3) ELEVATION - SIX different drop-shadow recipes with no scale, so a bottom sheet, a FAB and a popover each floated at an arbitrary different height. Collapsed to a three-step scale built from values already present: `--elev-1: 0 6px 18px/.32` (raised), `--elev-2: 0 10px 26px/.35` (floating), `--elev-3: 0 14px 36px/.4` (overlay), mapped by blur radius 2/2/2. The focus ring (`0 0 0 3px`), accent glow (`0 0 8px`) and key-tile inset hairline are NOT elevation and were left exactly as they were. (4) TYPE + SPACING - five one-off values folded onto the existing scale: `.qkey` 0.98->1rem, `.song-title` 1.04->1.05rem, `.chip` 0.76->0.8rem, `.ed-hint` **13.6px -> 0.85rem** (renders identically - this was px sitting in a rem scale, introduced by build 81's own hint fix), `.song-view h1` font-weight 800 -> 700, and `.row-tools` gap 5px -> 6px (the scale is 4/6/8/10/12). DELIBERATELY LEFT ALONE: the `1.8rem` FAB close glyph (an icon size, not text), every `em`-based sheet size (they scale with the `--sheet-size` control by design), `10.5pt` in the print block, all input font-sizes (16px iOS zoom floor), and `.brand` at font-weight 800 - a logo WORDMARK is the app's identity and is legitimately outside the text scale, so it is now the single documented exception. NOT DONE, a judgment call left open: the scale still holds 0.8/0.82/0.85/0.9/0.95rem - finer than it needs to be - but collapsing those visibly resizes text on many surfaces at once, unlike everything above. NOTE the preview browser forces `prefers-reduced-motion: reduce`, so durations compute to 1e-06s there; the CURVE was verified to resolve on every surface but real motion timing needs a normal browser or a device.
- **build 83** (2026-08-11) - PRINT IS NOW JUST THE SONG, and the editor header buttons became one family. (1) PRINT: the `@media print` block hid only `.fab-wrap`, `#progress`, `.keyline`, `.appfoot` and `.editor`, so a printed sheet still carried the Back/Songs button, the favourite star, the Tap-tempo button and the whole transpose pill. It now also hides `#song-view header`, `.song-header`, `#quickbar`, `#totop`, `#list-view` and a blanket `button`. The blanket rule is safe and deliberate: `#song-view` holds the chrome and `h1#song-title` / `.sheet` as SIBLINGS (the title is NOT inside `.song-header`), and the sheet contains zero `<button>` elements - only `.chord`/`.lyrics` spans - both verified live before shipping. A printed page is now title + chords + lyrics, nothing else. The existing two-column print rules, margins and `.sheet` print font-size are untouched. (2) HEADER BUTTON FAMILY: `#ed-save` (filled indigo, NO border), `#ed-delete` (transparent, danger text, tinted border) and `#ed-close` (circular `.iconbtn` with its own background) each looked self-designed. They now share one shape language - all three 44px (`--ctl-h`), all `--r-pill`, all 1px border (`#ed-save` gained a 1px border in its own accent so it cannot sit 2px shorter than the outlined ones), and the two text buttons match each other exactly at 15.2px/600. Role is carried ONLY by colour now, never by a different shape or weight. (3) DELETE HAS A REAL PRESSED STATE: it previously only ever rendered outlined, so a tap gave no confirmation. `#ed-delete:active` now fills solid `var(--danger)` with white text - `:active` is what a PHONE shows, since touch devices have no hover - and the matching `:hover` is wrapped in `@media (hover: hover)` so a touch device never keeps a sticky filled state (the pattern already used elsewhere in this file). Transition uses the existing `--t-fast`. White on `#b3243f` measures 6.46:1. No new colour tokens; the 12px gap holding Delete off Save is unchanged and nothing dropped below 44px.
- **build 82** (2026-08-11) - PHONE EDITING: stop iOS corrupting charts, and stop losing work. (1) iOS AUTOCORRECT WAS SILENTLY MANGLING CHORD CHARTS. `spellcheck="false"` (which both textareas already had) only suppresses the red squiggle - on iOS it does NOT disable autocapitalize or autocorrect. So every chord line got its first letter auto-capitalised after a newline, and tokens like `sus4`, `Bm7`, `maj7`, `N.C.` were live autocorrect targets, in the most-typed surface in the app. The correct pattern already existed in this file at `#cp-input` (the chord-popover editor) and had simply never reached the main editor. All eight editor fields now carry it: `#ed-text`/`#ed-text2`/`#ed-key`/`#ed-key2` get `autocapitalize="off"`, `#ed-tags` gets `none`, `#ed-name`/`#ed-lang`/`#ed-lang2` get `words` (titles and language names SHOULD capitalise), and every one gets `autocorrect="off" autocomplete="off" spellcheck="false"`. `autocorrect` is a non-standard WebKit attribute - intentional, inert elsewhere, and exactly what the existing `#cp-input` precedent does. (2) DRAFT AUTOSAVE - there was NO draft persistence anywhere (the only `visibilitychange` handler was for wake-lock), so an incoming call or an iOS tab eviction mid-edit lost the entire song being typed. One `eddraft` key holds `{uid, t, f:{...}}`, written on a 600ms debounce from ONE delegated `input`/`change` listener on `.editor-card`, and **flushed synchronously on `visibilitychange`->hidden** because a debounce timer does not survive the tab being backgrounded - that is the case that actually matters. On reopen, `showDraftBar()` shows an `ed-draft-bar` (`role="status"`, 44px buttons, existing tokens only) ONLY when a draft exists AND its uid matches the song being opened AND its fields DIFFER from what just loaded - so a stale identical draft never nags. Restore fills via the existing `fillEditor()` (so the bilingual block visibility follows `ed-biling` for free) and deliberately KEEPS the draft; Discard deletes it. The draft is cleared only on a SUCCESSFUL save (both the Supabase and the local-song paths) - never on a plain close, since surviving a close is the entire point. Verified live: autosave after the debounce, synchronous flush on hide, draft survives close, bar reappears with "1 minute ago", fields load CLEAN rather than being silently overwritten, Restore repopulates, Discard clears and stays cleared, and opening a DIFFERENT song shows no bar while preserving the original song's draft. NOT ADDRESSED, needs an on-device check: the editor sheet is capped with `100dvh` and the textarea is `min-height: 55vh`, and neither unit shrinks when the software keyboard opens (`visualViewport` is unused), so the keyboard can still overlap the lower sheet.
- **build 81** (2026-08-11) - EDITOR VISUAL PASS, which turned up an APP-WIDE contrast failure. (1) `--muted` was `#7c7264` on the light `#f4efe5` paper = **4.12:1**, below the WCAG AA 4.5:1 floor for normal text, and it is applied to **955 elements**: `.song-secondary` (the Ukrainian title on every bilingual row, 13.12px), `.song-langs` (10.56px), `.count`, `.hint`, plus the editor's own `.ed-hint`/`#ed-status`. Dark measured 6.13:1 and was already fine, so ONLY the light value changed, to `#736959` - now light 4.71 / dark 6.13 / stage 7.29. This is the single most visible part of the build: every muted string app-wide is slightly darker in the light theme. (2) `.ed-icon` (the Restore-previous button) broke THREE standing rules at once: 32x32 against build 62's 44px minimum, `--r-md` 12px radius while `#ed-close` sits directly beside it at `--r-pill`, and font-size 13.3333px because it was never assigned one - the same browser-default leak build 63 closed for `font-family`. Now 44x44, pill, explicit 1.1rem, SVG unscaled. (3) The six `.editor input`s computed 43px against the `--ctl-h: 44px` token; `min-height: var(--ctl-h)` lands them on exactly 44 with font-size still EXACTLY 16px (the iOS focus-zoom floor, build 62 - non-negotiable). (4) `.ed-check` rows were a 24px tap target (18px box); now 44px. (5) `.ed-hint` was the smallest AND wordiest text in the editor - 12.48px crammed into a 157px column beside the Format button, wrapping to 4 ragged lines; now 13.6px on its own full-width line below the button. (6) DESTRUCTIVE ADJACENCY: Delete was 77px wide against Save's 64px - the irreversible action had MORE thumb area than the primary one, immediately beside it. Delete is now transparent with danger-red text and a 20px gap from Save; `#ed-close` stays rightmost. A new `--danger` token backs it. FOUND WHILE VERIFYING, not planned: `.login-err` hardcoded that same red and therefore also measured 2.96:1 in light - a sign-in error message the user could not comfortably read; it now resolves through `var(--danger)` too, so the token's "ONE source" claim is actually true. TWO BUGS CAUGHT IN VERIFICATION that were reported as done: `.ed-hint` was edited but a SECOND `.ed-hint` rule later in the file overrode it at equal specificity (exactly the trap build 78 records for `.row-drag`) so the font-size never applied until the duplicate was deleted; and the danger red was adopted as `#e2607a` without computing its contrast, measuring **2.96:1** on the light card - a contrast-fix build very nearly shipping a fresh contrast failure. Light `--danger` is `#b3243f` (5.65:1); dark keeps `#e2607a` (5.68:1). Verified live at 375x812: zero undersized targets remain anywhere in the editor, all inputs 44px/16px, and muted contrast re-measured in light, dark and stage.
- **build 80** (2026-08-11) - LANDSCAPE TWO-COLUMN IS NOW EARNED, NOT ASSUMED. The `@media (orientation: landscape) and (min-width: 600px)` rule applied `column-count: 2` to every song, but two columns only help when the sheet then fits on screen ENTIRELY. If it does not, they are strictly WORSE than one column: multicol balances the content into two half-height columns, so you scroll a full column down and must scroll all the way back UP to start column two, instead of reading one continuous scroll. MEASURED before changing anything: at 1024x768 only 4 of 12 sampled songs fit in two columns (`I Thank God` needed 2326px per column, 3x the viewport); at phone-landscape 844x390 each column stood 2.06 viewport-heights tall, so EVERY song paid the penalty. (1) The column rules moved behind `html.fit2col` inside a media query that also requires `min-height: 600px` - a hard floor that keeps two columns off portrait and off phone-landscape entirely, so JS can only ever opt IN within it. `#app{max-width:1100px}` and the `.sheet` landscape padding stay unconditional (unchanged list behaviour). (2) New `fitColumns()` measures per song: switch columns on, keep them only if the sheet then fits the viewport; add + measure + revert happen inside ONE rAF callback, before paint, so a rejected layout is never shown. Re-runs from `renderSheet`, `applySize`, `applyChords`, the stage-mode toggle, `closeSong`, and window resize/orientationchange - text size, stage mode (1.5x) and rotation all change whether it still fits. TWO MEASUREMENT BUGS found and fixed by verifying in the browser rather than trusting the first version: `document.scrollHeight` LAGS a shrink while the page is scrolled down, so every mid-song re-measure read the stale one-column height and rejected; and `getBoundingClientRect().top` is DISPLACED by the song-open transform animation (`animateSheet`), which made the outcome a race - the same song accepted at one wait and rejected at another. Both replaced by an `offsetTop`/`offsetHeight` walk, which ignores transforms and scroll position. Verified: identical results at 250/600/1200ms waits (race gone); the fit/no-fit margins fall in a clean gap (-94px vs +107px) so the threshold is not knife-edge and needs no tolerance; size +1 keeps two columns, +3 drops to one, reset restores; stage mode drops to one and back; phone-landscape 844x390 and portrait 375x812 both stay one column; autoscroll still runs on a rejected (one-column) song; no console errors. PRINT IS UNTOUCHED - the `@media print` block carries its own unconditional `column-count: 2` and never consulted the screen rule.
- **build 60** (2026-08-06) - Song list rebuilt around a KEY TILE (branch `design/list-view-v2`). (1) Each row leads with a 44px `.row-key-tile` whose hue comes from the key's position on the CIRCLE OF FIFTHS (`keyHue()`, 30 deg/step: C=0, G=30, D=60...); minor keys take their relative major's hue at lower saturation, so Am and C read as siblings. Saturation/lightness come from per-theme vars `--key-sat/--key-lig/--key-fg`, with a desaturated dark set under `html.stage` so nothing glows on stage. Unknown/absent key -> `.neutral` grey tile. (2) Bilingual titles split onto `.song-primary` / `.song-secondary` lines instead of one run-on string; primary clamps to 2 lines. (3) Star/+ buttons removed from the resting row: swipe reveals them (`.row-shade-left` = Favourite, `.row-shade-right` = Add to set), reveal-not-commit so an accidental flick can't edit the set; horizontal drag only engages when |dx|>|dy| so vertical scrolling is never hijacked. `.row-desktop-actions` keeps real focusable buttons at >=641px because a gesture-only affordance is unreachable by keyboard and screen readers. (4) The build-59 alpha-nav strip was REMOVED. Row height 82px -> ~65px at 375px (about 9 rows visible vs 6). Verified live at 375x812: 112 rows render, all 21 distinct key tiles clear 4.5:1 contrast in light/dark/stage (min 8.24/5.94/9.59), swipe reveals the correct single side in both directions, `node --check` passes on all JS.
- **build 61** (2026-08-06) - List polish on top of build 60. (1) LANGUAGE BADGE CONSISTENCY: `.song-langs` was appended INSIDE `.song-secondary` for bilingual songs and given its own `.row-meta` line for single-language ones - two shapes for one piece of metadata, and the badge inherited the title's type. Now always its own `.row-meta` line, left-aligned with the title in both cases. `.row-meta` also gained a real CSS rule (it had none). (2) Light-theme key tiles were washed out next to dark: `--key-sat` 58%->72%, `--key-lig` 90%->85%, plus a new `--key-edge` hairline inset ring per theme so a pale tile still reads as an object. Contrast re-verified: light min 7.28, dark 5.94, stage 9.59 across all 21 keys. (3) Key tiles clip long keys (F#m/Bbm) instead of spilling 44px. (4) One-time swipe hint under the count, retired permanently on the first committed swipe (localStorage `swipehintdone`), hidden >=641px where real buttons exist, honours prefers-reduced-motion. Row heights: 65px single-language, 77px bilingual (12 of 112).
- **build 62** (2026-08-06) - MOBILE PASS across list/set/song/editor, audit-driven (measured, not eyeballed). (1) iOS FORCED ZOOM killed: iOS Safari zooms the whole page when a focused text field is under 16px, and #ed-text / #ed-text2 (0.9rem), #set-select (0.9rem) and #cp-input (0.95rem) were all under -- so tapping into the song editor, the most-typed surface in the app, zoomed the page every time. All four are now exactly 1rem/16px. (2) TOUCH TARGETS: every interactive control across all four surfaces now clears 44px. Fixed: .qbtn transpose 34->44 (the most-used control mid-service), .qbpm 42->44, .setbtn 42->44, .row-add/.row-fav/.row-drag/.row-remove 40->44 wide, .chip artist filters 27->44 tall, list-tabs 40->46 (primary navigation), .seg-clear 28->44 (destructive, sits beside the Set tab), .ed-btn/.ed-mini/.ed-link min-height 44. Verified 0 undersized targets remain in list, set, song and editor. (3) LANDSCAPE SAFE AREA: #app had padding for safe-area top/bottom but NOT left/right, so in landscape -- how songs are read on a stand -- the notch cutout could sit over the first chord of every line. Now inset on both sides. (4) .set-bar scrolls horizontally: six 44px controls do not fit 375px, which was crushing the set-name select to 'My se'. (5) Editor Key/Language split 108px/150px so the Language placeholder stops clipping mid-word.
- **build 63** (2026-08-06) - DESIGN SYSTEM pass: make the app read as ONE thing. Audit measured 6 border-radii, 8 control heights, 9 control font-sizes and 3 font families across list/set/song/editor. (1) THE ARIAL LEAK: form controls do not inherit font-family, so 27 text-bearing controls -- the transpose - and +, every close x, Format, Cancel, Log in -- silently rendered in the browser default beside Inter everywhere else. One `button,input,select,textarea{font-family:inherit}` rule closes the class permanently. Font families now 2 (Inter + JetBrains Mono), was 3. (2) CONTROL TOKENS in :root -- `--ctl-h:44px`, `--ctl-h-lg:52px`, `--r-pill:999px`, `--r-md:12px` -- so a new control cannot invent a fourth 'rounded' or a ninth size. (3) Transpose group and Tap tempo were 52px vs 44px SIDE BY SIDE (the .qt pill wraps 44px buttons in 3px padding + border); both now `--ctl-h-lg`, so the control row reads as one unit. (4) Radius collapsed 6 -> 4: inputs 10px, textareas 12px and ed-restore 8px all become `--r-md`; .fab/.totop 50% become `--r-pill` (.ed-dot stays 50%, it is a status dot not a control). (5) Orphan font sizes folded onto the scale: .qbpm 0.92rem->0.9rem, .qbtn 1.15rem->1.1rem. (6) FOCUS RING: `:focus-visible` appeared exactly ONCE app-wide and several inputs set outline:none, so keyboard and switch-control users had almost nothing to follow. Added element-qualified focus rules (higher specificity than the outline:none declarations) for button/input/select/textarea/a/[tabindex]. Also REMOVED `border-radius:6px` from the pre-existing :focus-visible rule -- it visibly squared off a pill button the moment it took keyboard focus.
- **build 64** (2026-08-06) - Song landing block compacted: it spent ~1/3 of the phone on chrome before the first lyric. MEASURED 272px (33% of 812) -> 158px (19%) for a one-line title, 183px (23%) for a two-line bilingual one. Nothing was removed and both transpose controls are kept (quickbar + FAB panel). (1) h1 1.75rem -> 1.3rem with line-height 1.2 and tight margins -- 1.75rem forced most titles onto two lines. (2) .song-header padding 14/10 -> 8/6; .quickbar gap 14 -> 10 and margin 14 -> 10; .keyline 0.9rem -> 0.82rem. (3) THE KEY LINE NOW EARNS ITS ROW: untransposed it read 'Original key A' while the transpose pill directly below already showed A -- a full row spent saying the same thing twice. It is hidden when delta === 0 and reappears the instant you transpose, when it becomes the only place the ORIGINAL key survives ('Original key A · now A# (+1)'); the keyless branch always shows it and clears the flag left by the previous song. NOTE for future work: the 33% figure was the LANDING state only. While scrolling/reading, the title and quickbar scroll away on their own and sticky chrome was already just 78px (10%) -- verified before changing anything, so no collapsing header was built for a problem that did not exist.
- **build 65** (2026-08-06) - Three song-view fixes measured at 375x812. (1) UNIFORM HEADER HEIGHT: bilingual songs used to print BOTH titles joined by " / " in the h1, so it wrapped to 2 lines (h1 25px vs 50px), giving 158px vs 211px sheetTop. The h1 now shows ONLY the active-language version's title (checked in `renderSheet`, so switching a lang tab updates it), clamped to exactly one line with a FIXED height (`calc(1.3rem*1.2)`). Every song now has an identical distance from screen top to first lyric. (2) TRANSPOSE NO LONGER SHIFTS THE SHEET: `.keyline` was in normal flow and toggled visible when delta!==0, pushing every lyric down 28px mid-song. It is now `display:none` permanently (out of layout). The transposition state moved into the transpose pill: the existing `#qt-reset` becomes a +/- delta badge when transposed, and its aria-label + title become "Reset to original key (A)" so the original key is never lost. SheetTop is byte-identical before/after a transpose. (3) CONTROL HEIGHT: `--ctl-h-lg` 52 -> 46px and `.qt` padding 3 -> 0, so the pill is a 44px inner button + 1px border = 46px outer; the inner `.qbtn` stays 44x44 (touch minimum, non-negotiable); `.qbpm` follows the same token, so transpose and tap-tempo stay exactly equal in height (46px).
- **build 66** (2026-08-06) - Role-aware UI + login polish. (FIX A) `#add-btn` (header +) and `#edit-btn` (FAB Edit) are now gated on `loggedIn()` inside `updateAdminUI()`, so a signed-out reader never sees them and can no longer open the editor only to fail on Save. The same refresh is called on login success, `logoutAdmin()`, the boot init, and the remember-me refresh resolve - controls track the session instantly, not just when the editor opens. RLS still gates writes; `ensureAuth()` still rejects unauthenticated saves. (FIX B) A discreet auth row at the FAB panel foot - the single sign-in entry always reachable while signed-out (since + / Edit are now hidden): "Sign in" -> opens the login sheet; signed in it shows the email + "Sign out". (FIX C) Login sheet polished: `#login-go` is now FULL-WIDTH and the only filled button; a `role="alert"` error line inside the sheet reports a failed login (no `alert()`, no silence); the button shows "Signing in…" and disables email/password while the request is in flight (no double submit); the email field autofocuses; a show/hide password toggle sits inside the field (44px, `aria-pressed`); Enter in either field submits. "Remember me" behaviour unchanged. All inputs stay >=16px (no iOS focus-zoom); all controls >=44px; light/dark/stage use existing tokens only (--ctl-h, --r-pill, --r-md, --accent, --fav) - no new colours. CORRECTION applied after live verification: the version-title check alone did NOT work -- both versions' ChordPro carry the SAME {title:} (the joined 'English / Ukrainian' string), so `cv.title` was always truthy and always the joined form; the one-line clamp then just ellipsised it ('A Ele A Glória / Йому За Все…'), showing neither title in full and leaving the lang tab with no effect on the h1. renderSheet now treats a version title that merely REPEATS the combined string as not-a-per-version-title and falls back to splitting cs.title on ' / ' (reusing the list's splitTitle helper), taking the half matching the active version. Verified live: English tab -> 'A Ele a Glória', Ukrainian tab -> 'Йому за все слава', neither truncated, and all three header variants (single / bilingual-EN / bilingual-UK) land at sheetTop 152px. Reset label also de-duplicated ('Reset to original key (A)', was '(original key A)'). CORRECTION applied after live verification: gating #edit-btn inside updateAdminUI() alone did NOT work -- showSong() and openSong() each ran `$("edit-btn").classList.toggle("hidden", !songs[i].uid)` AFTER it, re-showing Edit to signed-out readers every time a song was opened and silently undoing the auth gate. Both call sites now test `!songs[i].uid || !loggedIn()`. Verified live signed-out: + hidden, Edit hidden across three different songs, and the FAB Sign in row still reachable (44px) so an admin can never be locked out. Login sheet verified live: full-width 342px button, autofocus on email, Enter submits, password toggle flips type + aria-pressed + label, 'Signing in…' disables the button and recovers, and a failed sign-in renders a visible role=alert error ('Sign-in failed. Check your email and password, and that you are online.') which collapses to height 0 when empty.
- **build 67** (2026-08-06) - (1) UNIFORM LIST ROW HEIGHT: a bilingual row carries a third line (secondary title) and stood 77px against a 65px single-language row; the absolutely-positioned swipe shades inherit row height, so the reveal panels were visibly different sizes too (owner-reported by selecting .row-shade-left). `ul.list li` now has min-height 72px with padding tightened 10px->7px: EVERY row is 72px, bilingual or not, verified across 12 rows with zero clipped content. (2) SIGN IN FROM THE LIST: #list-auth-btn padlock added to the list header beside the theme toggle, wired to the same promptLogin()/logoutAdmin() door as the FAB row -- an admin lands on the list, not on a song, and should not have to open one to sign in. updateAdminUI() relabels it and toggles `.authed` (ACCENT + accent-soft) while signed in; deliberately NOT `.on`, which is the amber FAVOURITE state -- a signed-in padlock wearing it would read as 'favourited'. Verified: 44x44, visible when signed out, opens the login sheet from the list with email autofocused. (3) LOGIN SHEET CENTRED: `.editor` is `place-items: end center` (a bottom sheet), which suits the tall song editor but made the small login dialog cling to the bottom edge. Scoped `#login { place-items: center; padding: 16px }` plus all-four-corner 18px radius -- the song editor deliberately STAYS a bottom sheet, where a full-height form belongs and where the on-screen keyboard cannot cover it. Verified 270px above / 270px below at 375x812.
- **build 68** (2026-08-06) - (1) NO MORE ADMIN FLASH ON LOAD: #add-btn shipped WITHOUT `hidden`, so the + painted on first render and only vanished once updateAdminUI() ran -- signed-out readers saw an admin control blink at them every reload. It now ships `class="iconbtn hidden"` and auth REVEALS it, so the unsafe state can never paint. (#edit-btn already shipped hidden, hence no flash there.) Verified against the SERVED html, not just the runtime DOM. (2) SIGNED-IN IS NO LONGER BLUE: the list padlock used an accent colour + accent-soft background for the signed-in state, which competed with the amber favourite and read as decoration rather than status. The `.authed` rule is gone; the padlock now shows state by OPENING -- #list-auth-shackle swaps d between the closed 'M7 11V7a5 5 0 0 1 10 0v4' and the open 'M7 11V7a5 5 0 0 1 9.9-1'. The button is otherwise identical to every other .iconbtn (verified: same colour and background as #theme-btn).
- **build 69** (2026-08-06) - FIRST-PAINT CORRECTNESS + remember-me durability. The head already had a pre-paint script for the theme PALETTE, but three pieces of state were still resolved by app.js AFTER first paint, so each one visibly flashed on every reload: the theme button (markup shipped a &#9728; text glyph that applyTheme replaced with an SVG), the + (appeared a frame late for admins, blinked away for readers), and the padlock shackle. FIX: the pre-paint script now ALSO sets `html.is-admin` from the stored token+expiry, and all three are chosen by CSS off <html> attributes -- both theme icons and both padlock shackles ship in the markup and CSS picks one, so the FIRST painted frame is already correct and nothing is swapped afterwards. applyTheme no longer touches innerHTML; updateAdminUI now toggles a single `html.is-admin` class instead of per-button `hidden` (one source of truth; the old `.hidden` on #add-btn would have lost a specificity fight with `html.is-admin #add-btn`). The pre-paint admin class is PRESENTATION ONLY -- Supabase RLS remains the real gate on every write. REMEMBER-ME: refreshSession() had no single-flight guard, and Supabase ROTATES refresh tokens (single-use). Two overlapping refreshes -- the boot restore at init plus an ensureAuth() from a save -- meant the second sent an already-spent token, took the 400 branch, and that branch CLEARS sb_refresh, silently ending a remembered session for good. All callers now share one in-flight promise (refreshSession -> doRefreshSession). Verified live: session survives reload, + matches auth state, theme icon tracks the theme both ways, exactly one icon ever displayed.
- **build 70** (2026-08-06) - Four owner-reported micro-fixes. (1) SONG CONTROL ORDER: `.qbpm` (Tap tempo) now precedes the transpose `.qt` pill in `.quickbar`; both stay --ctl-h-lg (46px) and equal. (2) EDITOR SCROLL TRAP: a vertical drag starting on a lyrics textarea was captured by the textarea, so the editor sheet could not scroll until the field hit its edge. `#ed-text/#ed-text2` now get `touch-action: pan-x` while UNFOCUSED -- vertical gestures fall through to the sheet, horizontal chord-line panning stays inside the field -- and `touch-action: auto` on `:focus` so editing again pans normally. `white-space: pre` unchanged. (3) FAB PANEL COMPACTED: `.fab-panel` padding 12->10 and column gap 8->6, `.fab-row` gap 6->4; every `.fbtn` stays 44px (touch minimum unchanged), no control removed. (4) FAB SIGN-IN ROW SIMPLIFIED: the "Admin access"/email second-line subtitle is gone; one clean line -- "Sign in" signed out, "Sign out · [email]" signed in (`.fab-auth-email` quiet inline). Still a real 44px `<button>`, same `.fbtn.flex` language as the other rows.
- **build 71** (2026-08-06) - SWIPE DECISION (owner-decided). (1) ROW SWIPE IS NOW ONE STEP: `enableRowSwipe`'s `end()` fires the action directly on commit -- right swipe = favourite toggle, left swipe = add/remove from set -- then springs the row back to translateX(0) with no lingering revealed state and no second tap. The shade stays visible during the drag (feedback), and after commit the revealed (now updated) shade holds ~260ms so the user sees what changed before it springs home. |dx|>|dy| guard unchanged (vertical scrolling never hijacked); `suppressSwipe`/`_suppressClick` still stop a swipe from opening the song. The shade-button `click` handlers were refactored into shared `doAdd`/`doFav` closures (passed as `{fav, add}`) so the swipe and any residual tap do the same thing. (2) ALL<->SET SWIPE REMOVED: the list-container horizontal gesture that switched tabs is gone (`swipeStart`/`swipeEnd` now early-return on `current === null`); tapping the bottom All/Set tabs is the only way to switch, so a list swipe can never fight the row actions. The song-view next/prev swipe is unchanged. (3) `.row-desktop-actions` buttons at >=641px remain the keyboard/screen-reader path. The one-time swipe hint text is untouched.
- **build 72** (2026-08-06) - Three corrections. (1) EDITOR SCROLL TRAP was fixed INVERTED in build 70: the textarea got `touch-action: pan-x`, which permits the HORIZONTAL gesture and withholds the vertical one -- exactly the trap being reported. Now `pan-y` while unfocused (vertical drag reaches the editor sheet, field cannot hijack it sideways) and `auto` on :focus for chord-line panning while editing. (2) LOGIN CLEARS THE KEYBOARD: the centred dialog sat under the iOS/Android keyboard (~bottom 45%); `#login` is now `place-items: start center` with `padding: 8vh 16px 16px`. (3) The FAB sign-in row no longer renders the signed-in email -- noise in a controls panel; the aria-label still carries identity for assistive tech.
- **build 73** (2026-08-06) - Swipe-to-favourite LOOKED broken on a phone but was working all along: it toggled storage correctly and showed NOTHING, because the only favourite affordances were .row-fav (desktop-only, hidden under 641px) and the swipe shade (which springs away on commit). Added a persistent .row-fav-mark star in .row-meta, rendered from favHas() and updated live by doFav() so the swipe gives immediate lasting feedback. Add-to-set never had this problem because the Set tab badge changes.
- **build 74** (2026-08-06) - (1) The swipe star now FILLS amber on commit (.row-shade-btn.fav.on svg), matching the + -> checkmark confirmation on the other side. (2) Live list update: with the Favourites filter active, un-favouriting now removes the row (renderList deferred 300ms so the rebuild does not yank it out mid spring-back); previously the list kept showing a song that no longer matched the filter.
- **build 78** (2026-08-06) - SET VIEW READS AS A RUNNING ORDER, not a filtered catalog. (1) POSITION NUMBERS: every Set row leads with a 1-based `.row-pos` (quiet mono, 22px fixed width so 1->10 never shifts the key tile; `aria-label` "Position N in the set"). Order keeps the key tile as the colour anchor; numbers renumber on remove (re-render) and after a drag-reorder via a new `renumberSetRows()` called from `beginCloneDrag.end()` (covers both the mouse grip and press-and-hold touch paths, which share that core). (2) DIFFERENTIATOR: the count line reads "N songs in this set" in Set view (the All/catalog-phrasing "N songs" is untouched, and search-from-the-Set-tab still shows the catalog phrasing since it is not setView). No added chrome - position numbers + running-order count are the quiet signal, reusing existing tokens (--muted, JetBrains Mono). (3) Row height stays a uniform 72px (the number sits inline, adds no line). No duration/length estimate added (only BPM is stored; a total would be invented). Drag-reorder, remove, tap-to-open, Set/All tabs, search-from-Set, and All-only row swipe all preserved. FOLLOW-UP after live check: position numbers + tools squeezed the title column to 132px (vs 271px in All songs), truncating 'Blessed Assurance' to 'Blessed...'. The drag GRIP is redundant on touch (enableHoldDrag reorders by press-and-hold anywhere on the row), so it is hidden under 641px and kept for mouse users above it -- title recovered to 181px, no truncation, remove stays 44x44. NOTE the rule needs `ul.list li .row-drag` specificity: a plain `.row-drag` media rule placed earlier in the file loses to the base declaration at equal specificity.
- **build 79** (2026-08-11) - The hint line is now dual-purpose: All view teaches swipe-for-actions, Set view teaches 'Press and hold a song to reorder'. That gesture had NO affordance on a phone since build 78 hid the visible drag grip under 641px. Suppressed while searching from the Set tab, because a search there returns CATALOG matches (swipeable) rather than the stored set order.
- **build 77** (2026-08-06) - Three fixes. (1) EDITOR SCROLL TRAP - REALLY FIXED: build 72's `touch-action: pan-y` alone was insufficient - pan-y permits the vertical gesture but does not pick its receiver, which is the nearest scrollable ancestor, and the ~447px tall overflow-y textarea IS that ancestor, so a vertical drag still scrolled the field's own content and never reached the editor sheet. Now `#ed-text/#ed-text2:not(:focus)` get `overflow: hidden` (nothing to scroll -> the gesture propagates to the sheet) with `overflow: auto` restored on `:focus` (edit + scroll + horizontal-pan the field normally). `touch-action: pan-y`/`auto` kept; `white-space: pre` and typed content unchanged when focus moves. (2) DESKTOP HEADER JUMP 10px: with an empty set #list started at y=268 and jumped to y=278 once the set gained its first song - `#set-clear` was toggled via the `hidden` class (display:none), so the tabs row grew. It now ships without `hidden` and empty state is `visibility:hidden + pointer-events:none` (.seg-clear.empty) which reserves its layout slot, so the tabs row height is byte-identical empty vs populated; when empty it is also `aria-hidden` + `tabindex=-1` (unfocusable, hidden from assistive tech), restored when the set has songs. (3) SWIPE HINT IS PERMANENT: the one-time retirement is gone. `retireSwipeHint()` and the `swipehintdone` localStorage key are deleted, and `syncSwipeHint()` no longer reads that key - so the hint always shows in the All view when there are rows, including for users who already have `swipehintdone` stored (it is never read). Hidden in Set view and >=641px, and honours prefers-reduced-motion, all via the existing CSS. Wording/styling unchanged.
- **build 76** (2026-08-06) - SONG SHEET TYPOGRAPHY: the surface a worship team reads at arm's length on a stand. (1) CHORDS NOW OUTSIZE THEIR LYRICS: `.sheet .chord` was 0.8em (12.8px vs 16px lyrics, ratio 0.80). Now `font-size: 1.05em` - relative to the sheet font size, never fixed px, so #size up/down and stage mode (which multiplies `.sheet` by 1.5) inherit the same >=1.0 ratio automatically. Colour unchanged (`var(--accent)`), so light-theme chord contrast stays 5.49:1 (the page's weakest text) - not weakened. (2) SECTION LABELS OUTRANK CHORDS: `.sheet .comment` grew 0.92em -> 1.15em (larger than the 1.05em chords) and gained a quiet hairline rule above (`border-top: 1px solid var(--border); padding-top: 0.5em`) chosen over a tint band so it can't read as a button and doesn't fight the chorus tint. `.comment.note` standalone annotations are exempt (stay small, no rule). (3) INTER-SECTION GAPS HALVED: `.sheet .paragraph` margin-bottom 1.5em -> 0.75em (~32px -> ~16px) to buy back scrolling across ~84 spacer rows; section labels still keep the sheet from running together (their own 0.6em top margin + hairline carry the separation).
- **build 75** (2026-08-06) - Removing the LAST favourite while the Favourites filter is active now clears the filter and returns you to All songs, instead of leaving an empty filtered list on screen. Removal delay raised 300ms -> 650ms so the row is seen to un-star before it disappears rather than vanishing under your thumb.
- **build 58** (2026-08-05) — Added **Pre-Verse** and **Pre-Bridge** section labels (plus Ukrainian/Russian variants `передкуплет`/`передзаспів`, `передбридж`) alongside the existing **Pre-Chorus**, in both the app's `SECTION_EN` detector (`app.js`) and the converter's `SECTION` map (`convert-core.js`). `headerKey` already strips the hyphen, so `Pre-Verse`→`preverse` etc. map cleanly. Verified with the converter: `Pre-Verse`/`Pre-Bridge`/`Pre-Chorus` all render as `{comment:}` labels.
- **build 57** (2026-07-24) — Three fixes. (1) **Format button no longer mangles `Verse 1 x2`**: the repeat regex allowed a space in the `2x` form (`\d+\s*[xх]`), so `Verse 1 x2` (verse 1, played twice) matched `1 x` as the repeat and became `Verse 2 x1`. Now the `2x` form must be adjacent (`\d+[xх]`), so the section number and a separate `x2` repeat both survive - fixed in both `englishLabel` (display) and `headerLabel` (converter/Format). (2) **Universal search**: a search query now spans the whole catalog even from the **Set** tab (it used to only search songs already in the set). `renderList` uses `setView = listMode==='set' && !q` - a query on the Set tab lists all matches with the add-to-set `+` button; clearing it returns to the reorderable set. (3) **BPM control restyled to match the transpose group**: same pill shell (surface/border/999px radius), same 42px height, and the `♩ N BPM` value now uses the key's accent colour (`.qbpm.has-bpm`) so the two read as one unit. Verified in a real browser: `Verse 1 x2` stays intact, a Set-tab search returns 61 catalog matches with `+` buttons, and the quickbar renders as a matched pair in light + dark.
- **build 56** (2026-07-23) — Notes redesign per feedback. (1) **Inline notes**: write the note on the section header itself - `Verse 1 *softly` or `Chorus x4 *3rd accapella` - and it renders as a small italic muted note right next to the label (new `.note-inline` span). A `*note` line placed right after a header folds into that header too. A `*note` on its own with no header above still renders as a standalone block note. (2) **Chorus notes no longer trapped in the tinted card**: the note is emitted as part of the label's `{comment:}` BEFORE `{start_of_chorus}`, so it sits beside the CHORUS label instead of inside the colored chorus body. (3) **Less gap**: the note hugs the section (no extra blank inserted before it). Verified in a real browser: `Verse 1 *somethin` and `Chorus x4 *3rd accapella` render the note inline beside the label, and the chorus card no longer contains the note.
- **build 55** (2026-07-20) — Three refinements. (1) **Notes/annotations**: a line starting with `*` (e.g. `*softly, building`) renders as a small italic muted note under/inside its section (Verse/Chorus/Bridge) instead of lyrics; `standardize` emits `{comment: *...}`, `renderSheet` styles those as `.note` and drops the `*`, `tidyBlanks` keeps it hugging the section (no blank before). Works inside a chorus body too. (2) **Autoscroll slower**: BPM factor `0.42 → 0.25` px/sec-per-BPM (still scaled by text size and trimmable with slow/fast). (3) **Tap-tempo button fixed size**: `#qt-bpm` is now a fixed 118px centered pill so the label swap (🥁 Tap tempo / ♩ N BPM) no longer resizes it. Verified in a real browser: notes styled + `*` stripped, section labels unaffected, button width stable across taps.
- **build 54** (2026-07-20) — Moved **tap-tempo to the song header** (next to the quick-transpose bar) for easy live access, and removed the BPM field/button from the edit menu. The `♩ N BPM` chip (`#qt-bpm`) is now a button: tap it to the beat, it computes the BPM (averaging recent taps), updates autoscroll immediately, and ~1.6 s after the last tap persists `{tempo:N}` to the song (Supabase for admins, device for local songs; session-only if not logged in). The editor keeps the BPM in a hidden `#ed-bpm` input so editing a song no longer needs a visible field yet still round-trips the tempo. Verified in a real browser: header shows "🥁 Tap tempo" / "♩ N BPM", tapping sets the rate, and edits preserve the BPM.
- **build 53** (2026-07-20) — Two features. (1) **Quick transpose bar** in the song header (`#quickbar`: −/current-key/+/reset), mirrors the FAB key controls so you don't open the bubble for the common action; reset appears only when transposed. (2) **Tempo / BPM + BPM-driven autoscroll**: editor gets a BPM field + **tap-tempo** button (averages recent tap intervals); saved as a `{tempo:N}` directive in the ChordPro (`buildChordPro` injects, `chordproToSheet`/`normalize` read it - no DB migration). The song header shows `♩ N BPM`. Autoscroll is now **time-based** (frame-rate independent) and, when a song has a BPM, its speed is `bpm×0.42×size×scrollmult` px/sec (faster songs scroll faster); the slow/fast buttons adjust the `scrollmult` trim (remembered). No BPM → previous default speed. Verified in a real browser: quick transpose changes key+sheet, BPM shows, tap-tempo ≈ tapped rate, autoscroll scales with BPM (60→~25px/s, 180→~75px/s), editor round-trips the BPM.
- **build 52** (2026-07-20) — Fix: clicking "Format pasted text" once squished a verse into the following chorus (needed a second click). `chordproToSheet()` now forces exactly one blank line before every section label (idempotent), so a single Format is enough. Also updated docs: GitHub Pages now deploys from **`main`** (merged with `kind`; keep both in sync).
- **build 51** (2026-07-20) — Formatting polish across display + converter: (1) repeat markers normalise to `xN` in any notation (`2x`/`х2`/`(2x)` → `x2`) in section headers and on chord lines; (2) `N.C.` (and `NC`/`(N.C.)`) is recognised as a chord and shown in the chord row as `[N.C.]` (ChordSheetJS renders/transposes it fine); (3) blank lines only appear BETWEEN sections - `standardize` drops intra-section blanks (chorus-body reader now skips blanks and ends only on the next header) and re-adds one before each header, converter mirrors it with `tidyBlanks`; (4) added **Alt Chorus** (alternative/alternate chorus) as a section, and made `convert-core` translate section labels to universal English too (was display-only). Verified in a real browser: `Приспів 2x`→`Chorus x2`, `Alternative Chorus`→`Alt Chorus`, `Куплет 3x`→`Verse x3`, `[N.C.]` renders, and a stray blank inside a chorus is merged.
- **build 50** (2026-07-20) — "Format pasted text" now leaves the editor in the **chords-over-lyrics** layout (not raw ChordPro): after `smartImport()` it runs the result back through `chordproToSheet()`, the same reverser used when editing an existing song. So every song - however pasted (ChordPro, chords-over-lyrics, or the mangled per-line copy) - edits as tidy chords-over-lyrics, and the song view already renders every song that way. On save, `buildChordPro()` converts the layout back to ChordPro for storage. Verified in a real browser for interleaved + ChordPro inputs.
- **build 49** (2026-07-20) — Two things. (1) **Paste-import**: a "Format pasted text" button in the editor (both language fields) turns raw copied text into clean ChordPro via `ChordConvert.smartImport()`, and moves a detected title/key into their fields. Handles ChordPro as-is, aligned chords-above-lyrics (`convert()`), and the "mangled" website copy where every chord and lyric fragment lands on its own line (`reconstructInterleaved()` — holds each chord and attaches it to the following word, starts a new line on a capitalised fragment, brackets Intro/bar lines, infers the key). `CHORD_RE` widened for `Ab(add4)`, `Db2/F`, etc. Verified end-to-end in a real browser (interleaved + aligned + ChordPro all convert and render correctly). (2) **Removed em dashes** project-wide (README, docs, package.json, UI strings), replaced with hyphens; the two regex character-classes that intentionally strip em dashes from chord lines were left intact. Also removed the stray `"type":"module"` from package.json (this is classic-script + CommonJS, not ESM).
- **build 48** (2026-07-20) - Repo professionalism / metadata: added `package.json` (name, version, description, keywords, homepage, repo, MIT license, `check`/`serve` scripts, `private:true`, no deps), `.gitignore`, `.nojekyll` (Pages serves files as-is), and a fuller `manifest.json` (`id`, `lang`, `dir`, `categories`). README got shields.io badges (MIT / PWA / no-build / Pages), the live URL, and a "Tech" section. SW bumped because `manifest.json` is precached. (Repo description, topics, and homepage are Settings-only - set them in GitHub's UI; values provided in the PR/notes.)
- **build 47** (2026-07-13) - Favorites now survive a song rename: `renameInFavs()` remaps the star to the new title when a song is renamed (called from `saveEditor`'s `renamed()` alongside the existing `renameInSets()`). Closes the real gap behind the "reference songs by id" idea - sets were already rename-safe via `renameInSets`, favorites were not. (A full internal switch from title- to uid-keyed sets/favorites was scoped but deferred: it would also have to rework the cross-device share/import format, which is currently keyed on the stable global `num`, and can't be fully verified without live Supabase - the risk to the core sets/share features outweighed the modest remaining benefit. Titles still have two edge cases uid-keying would fix: a global rename only updates favs/sets on the device that made the edit, and two songs sharing an exact title are indistinguishable.)
- **build 46** (2026-07-13) - Definitive fix for the Android back gesture exiting the app: the app no longer auto-opens your last song at launch. Root limitation (confirmed on-device): a history entry created during page load - before any user gesture - is voided by Android Chrome, so an *immediate* Back swipe after launching into a resumed song exited the app (it only worked after waiting a few seconds; builds 42–43's deferred/synchronous push both failed this case). Now `restoreOpen()` resumes to the **list** and scrolls the remembered song into view with a brief highlight (`.just-open`), a one-tap reopen. Back from the list exits (expected); tapping a song pushes a real user-gesture history entry so Back reliably returns to the list. Verified with Playwright (mobile emulation).
- **build 45** (2026-07-13) - Two small wins. (1) Accent-insensitive search: `deaccent()` strips combining diacritics (NFD) from both the stored `searchText` and the query, so "gloria" finds "Glória" and "cafe" finds "Café" (distinct Cyrillic letters like и/і are left alone - that's a letter substitution, not a diacritic). (2) `refreshGlobal()` skips the `build()` + `renderList()` when the fetched catalog equals the cached copy the list was already built from - no more full DOM rebuild/flash on every launch when nothing changed. Verified in a real browser: accented + unaccented + lyric search all match, list intact.
- **build 44** (2026-07-13) - Perf: lazy ChordPro parsing. `normalize()` used to run the ChordSheetJS parser on *every* song on every load AND every `refreshGlobal` (~every launch), but the list only needs title/key - now read by regex (verified byte-identical to the parser across all 98 catalog texts). The parse is deferred to `ensureParsed(v)`, run only when a song is opened. Measured ~77 ms of parse per load/refresh on desktop Node eliminated from the list path (~250–400 ms on a phone). No behavior change: verified list titles/key badges, song render, transpose, chord popover/edit, and search all intact in a real browser.
- **build 43** (2026-07-13) - Reworked the Android back-gesture fix (build 42 didn't hold). Build 42 deferred the song's history push until the first interaction, but when the user opens the app and *immediately* swipes back - the first action being the Back gesture - no push had happened yet, so the app exited. Now `restoreOpen()` pushes the buffer **synchronously at load**: it labels the page-load entry as the list and pushes the song entry on top, so Back always lands on the list. Verified with Playwright (mobile emulation) across four paths incl. zero-interaction back (previously exited to about:blank, now shows the list).
- **build 42** (2026-07-07) - Android back gesture no longer exits the app when you launch straight into a resumed song. Cause: `restoreOpen()` pushed the song's history entry during load, before any user gesture, so Chrome's back/forward "history manipulation" intervention flagged it skippable and the first Back skipped it out of the app. Fix: on restore, show the song immediately but defer the `pushState` until the first user interaction (`pointerdown`/`touchstart`/`keydown`/`wheel`), which carries the activation that makes the entry stick; `closeSong()` and the in-app Back/brand buttons disarm the un-pushed buffer and close directly. Tap-to-open (already a user gesture) is unchanged. Verified with Playwright (mobile emulation): resume→interact pushes the buffer and Back returns to the list; in-app Back before any interaction closes to the list without exiting; normal tap-open + Back still works.
- **build 41** (2026-07-07) - Set import no longer duplicates: `importFromText()` (used by the QR/link auto-import and the paste-link prompt) now checks for an existing set with the same name AND identical song list (order included); if found it just switches to it (prompt path shows "You already have … - switched to it") instead of piling up copies on every re-scan. A set that differs in name or songs still imports as new. Verified with Playwright across both import paths.
- **build 40** (2026-07-07) - Song titles display in Title Case: `text-transform: capitalize` on `.song-title` (list rows, All + Set) and `.song-view h1` (open-song header). Display only - the stored/DB title is unchanged, so search and set membership still match the original text. Works for Cyrillic too (browser capitalizes per word); note it also capitalizes minor words and words already intentionally lowercased.
- **build 39** (2026-07-07) - maj7 chords now display conventionally: ChordSheetJS's HtmlDivFormatter renders `Amaj7` as `Ama7`, so `fixMaj()` restores `maj` in the song view (and the chord popover) - display only, the stored ChordPro keeps `maj7`. Precise (`ma` is always followed by a digit) so minor `m7` and `madd9` are unaffected; follows transpose. Verified in a real browser: `Cmaj7/Fmaj9` shown correctly, `Am7/Dm7` untouched, `+2` → `Dmaj7/Gmaj9`, popover + diagram intact.
- **build 38** (2026-07-07) - Editor: removed click-outside-to-close on the backdrop. Real cause of the "kicked out of edit" reports: a text selection dragged onto the dimmed backdrop released there, firing the backdrop's click handler and closing the editor (losing unsaved edits). The editor now closes only via the Close button / Save / Delete (mobile back gesture still works). Verified with Playwright: drag-select released on the backdrop keeps the editor open + text intact; a plain backdrop click no longer closes; Close button still closes and leaves the song open.
- **build 37** (2026-07-07) - Two fixes. (1) Desktop editor no longer gets discarded: a stray back gesture (overscroll while drag-selecting text) used to fire popstate and close the editor + jump to the previous song, losing unsaved edits; on `hover+fine` pointers the back is now ignored (editor stays open, close via the Close button), plus `overscroll-behavior` guards on `<html>` and the textareas block the swipe-back at the source. Mobile back-to-close is unchanged. (2) Chords placed over the space between words no longer merge the words ("Hold me[G] close" rendered "Hold meclose"): `transformLine()` keeps the word-gap space when the chord is glued to the previous word, and `renderSheet()` converts each lyric column's edge spaces to nbsp so the browser doesn't trim them at the flex boundary. Verified in a real browser (Playwright): correct spacing on three chord-over-space patterns, desktop edit survives a stray back, mobile back still closes, chord-edit round-trip intact.
- **build 36** (2026-07-06) - Diagram coverage: worship "2" chords (Ab2/C2…) now render (movable R-5-R-9-5 grip on the A string + full add9 barre on the E string; open Cadd9 = x32033); power chords ("5"); slash chords get real voicings - 22 named grips (C/E, D/F#, E/G#, G/B, A/C#, Am/G, Bb/D, Ab/C, Eb/G…) plus a generic first-inversion shape for any major chord with the 3rd in the bass (x-b-(b−2)×3-x on the A string); other slash chords still fall back to the base-chord shape.
- **build 35** (2026-07-06) - Chord popover: tap a chord in the song view → glass card with the chord name + guitar diagram (new `chord-diagrams.js`, offline, theme-aware via currentColor); admins (or local songs) get a pencil to edit the chord in place - saved to Supabase with `prev` backup, correctly untransposed when the view is transposed. Popover anchors near the tapped chord, clamped to the viewport; backdrop tap / Escape closes. Verified with Playwright: diagram render, edit persistence, transposed-edit math (D→stored C at +2).
- **build 34** (2026-07-06) - Perf + deploy visibility: `songs.js` served cache-first by the SW (saves ~135 KB per online load; it's only the offline seed, a `CACHE_VERSION` bump re-precaches it); `preconnect` to Supabase in `<head>` (faster first catalog fetch); "App updated - tap to reload" glass toast shown on SW `controllerchange` (skipped on first visit, auto-hides in 15 s, sits above the phone bottom-nav, z-index 70). Toast CSS lives at the end of the `<style>` block; logic in the inline SW-registration script at the end of `<body>`.
- **builds ≤33** - see git history (`git log --oneline`): flat/borderless list, glassmorphism chrome, bottom-nav All/Set tabs with swipe, press-and-hold set reorder, stage mode, bilingual editor, sets + share QR, favorites, tags, transpose/save-key, autoscroll, PDF print, Supabase catalog with auth + restore-previous.
